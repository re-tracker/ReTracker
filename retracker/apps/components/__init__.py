"""App-level optional components.

These are higher-level building blocks used by runnable apps (e.g. detection,
segmentation). They are intentionally separate from `retracker.models.*` to
avoid confusing app components with neural network modules.
"""

__all__ = []
